# Documentation build requirements for COBI
# Core Sphinx and theme
sphinx>=5.0.0
sphinx-rtd-theme>=1.0.0
nbsphinx>=0.9.0
sphinx-autodoc-typehints>=1.19.0

# Minimal dependencies for documentation build
# Heavy packages are mocked via autodoc_mock_imports in conf.py
numpy>=1.22.0

# All heavy dependencies are mocked during doc build:
# - healpy (mocked)
# - pymaster (mocked) 
# - pysm3 (mocked)
# - camb (mocked)
# - mpi4py (mocked)
# - curvedsky (mocked)
# - ducc0 (mocked)
# - emcee (mocked)
# - getdist (mocked)
# - scipy (mocked)
# - matplotlib (mocked)
# - astropy (mocked)
